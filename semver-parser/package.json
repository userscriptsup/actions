{
	"name": "semver-parser",
	"description": "Parse a SemVer string or Tag Ref",
	"author": "Userscriptsup",
	"license": "ISC",
	"version": "1.0.0",
	"private": true,
	"sideEffects": false,
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"dev": "ncc build src/main.js --watch --source-map",
		"build": "ncc build src/main.js --minify --source-map --license licenses.txt"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/userscriptsup/actions/semver-parser"
	},
	"dependencies": {
		"@actions/core": "^1.10.1"
	},
	"devDependencies": {
		"@vercel/ncc": "^0.38.1",
		"semver": "^7.6.2"
	}
}
